<UserControl x:Class="TiendaP.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TiendaP.View"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI" 
             xmlns:viewmodels="clr-namespace:TiendaP.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">

    

    <Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
                <TextBlock Text="Product Filter" Margin="0,0,20,0"/>
                <CheckBox IsChecked="False" Content="T-Shirts"/>
                <CheckBox IsChecked="False" Content="Shoes"/>
                <CheckBox IsChecked="False" Content="Shovels"/>
            </StackPanel>



            <ItemsControl Grid.Row="1" Name="ProductosLista" Loaded="ItemsControl_Loaded" ItemsSource="{Binding lista}" Grid.ColumnSpan="3">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Resources>
                    <!-- Estilo para el contenedor StackPanel -->
                    <Style TargetType="StackPanel">
                        <Setter Property="Orientation" Value="Horizontal"/>
                        <Setter Property="Margin" Value="1"/>
                    </Style>

                    <!-- Estilo para el botón Add to Cart -->
                    <Style TargetType="Button" x:Key="AddToCartButtonStyle">
                        <Setter Property="Content" Value="Add to Cart"/>
                        <Setter Property="Background" Value="#1E88E5"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="MinWidth" Value="100"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="HorizontalAlignment" Value="Right"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Resources>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <!-- Estilos de TextBlock e Image omitidos para mayor claridad -->

                            <StackPanel Orientation="Horizontal">
                                <Image Name="imagenValue" Source="{Binding ImagenURl}" MaxWidth="100" MaxHeight="100"/>
                                <StackPanel>
                                    <TextBlock x:Name="NombreValue" Text="{Binding Nombre}"/>
                                    <TextBlock x:Name="TallaValue" Text="{Binding Talla}" FontStyle="Italic"/>
                                    <TextBlock x:Name="PrecioValue" Text="{Binding Precio}" FontWeight="Bold"/>
                                </StackPanel>
                                <Button Content="Add to Cart" Style="{StaticResource AddToCartButtonStyle}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>

</UserControl>
